import React, { Component } from "react";

import SearchHomeLoadable from "../../loadables/search_loadable";

export default class SearchHome extends Component {
	render () {
		return (
      <div>
        <SearchHomeLoadable/>
      </div>
		);
	}
}
